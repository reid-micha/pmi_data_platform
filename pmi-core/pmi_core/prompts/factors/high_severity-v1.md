# Factor: high_severity (v1)

`value=1` if a YES resolution implies **mass-casualty** or **strategic-asset** events (nuclear use, capital city seizure, > 10k casualties, regime change). Skirmish / boundary incident → 0.

Return JSON: `{ "value": 0|1, "confidence": 0.0..1.0, "reasoning": "..." }`.

Title: {market_title}
Description: {market_description}
