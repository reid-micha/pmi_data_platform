# Factor: state_actor (v1)

`value=1` iff the conflict involves at least one recognised sovereign state on either side. Non-state vs non-state alone → 0.

Return JSON: `{ "value": 0|1, "confidence": 0.0..1.0, "reasoning": "..." }`.

Title: {market_title}
Description: {market_description}
