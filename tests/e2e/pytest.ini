[pytest]
testpaths = .
# E2E suite always runs serially — the Docker stack is shared session state.
addopts = -p no:cacheprovider -v
filterwarnings =
    ignore::DeprecationWarning
markers =
    e2e: end-to-end test against a live docker compose stack
